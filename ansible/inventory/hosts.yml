---
all:
  children:
    ninerouter:
      hosts:
        # Replace with your target. Any host reachable over SSH with sudo.
        ninerouter-01:
          ansible_host: 10.0.0.10
          ansible_user: ubuntu

      # Per-group overrides live in group_vars/ninerouter/. Keep secrets in the
      # vault file there, not here.
      vars:
        ninerouter_data_path: /var/lib/9router
